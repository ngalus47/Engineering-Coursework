1. Which BuildPSST() formal parameter defines an instance of the problem?
        A: n

2. If count is an OUT or IO parameter, must the actual parameter in main() be the address-of an integer variable?
        True

3. n=5 count?
        3125

4. n=6 count?
        46656

5. n=7 count?
        823543

6. What is the functional relationship between n and count?
        count = n^n

7. The need for #include <ctype.h> is due to the reference to toupper() in the actual-parameter expression in main()?
        True

8. Are (displayPartialPaths) and (displayPartialPaths == true) functionally equivalent?
        True

9. What does printf("%*c>m = %d\n",2*m+1,'*',m) output when m=2?
        "*****>m = 2"  (5 asterisks, then ">m = 2")

10. What does printf("%-*c>m = %d\n",2*m+1,'*',m) output when m=2?
        "*    >m = 2"  (1 asterisk left-justified in a 5-wide field, then ">m = 2")

11. Re-write *count += 1 using post-increment:
        (*count)++

12. Is the curly-brace pair absolutely necessary in the if-statement's then-clause?
        True

13. Is the PSST generated by Problem67.c a full tree?
        True

14. Is the PSST generated by Problem67.c a complete tree?
        True

15. Is a perfect n-ary tree both full and complete?
        True
